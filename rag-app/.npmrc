# Allow legacy peer dependencies for packages with outdated peer dependency requirements
# Required for: @glideapps/glide-data-grid (requires marked@^4.0.10, we have marked@^16.2.1)
legacy-peer-deps=true
